/** Stand-in for `@zcentral-v2/constants`. */

/** Tailwind's default breakpoints, which the theme's @theme block extends. */
export const BREAKPOINTS = {
  SM: 640,
  MD: 768,
  LG: 1024,
  XL: 1280,
  XXL: 1536,
} as const;

export const PAGINATION_ITEMS_PER_PAGE_OPTIONS = [10, 25, 50, 100];
